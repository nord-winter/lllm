# LLM API и UI - Qwen2.5-0.5B (Легкая и стабильная версия)

Проект представляет собой API и веб-интерфейс для работы с языковой моделью Qwen2.5-0.5B с расширенной диагностикой и автоматическим fallback на альтернативные модели.

## 🔧 Ключевые исправления

### Основные исправления:
1. **Смена модели**: Переход на легкую Qwen2.5-0.5B (494M параметров)
2. **Множественные fallback**: DialoGPT-medium → GPT2 для максимальной надежности  
3. **Правильная обработка токенов**: Автоматическое управление pad_token
4. **Детальная диагностика**: Полное логирование и диагностические эндпоинты
5. **Универсальная совместимость**: Работа с любыми доступными моделями

### Диагностические возможности:
- 📊 **Статус API**: Детальная информация о состоянии системы
- 🧪 **Тест модели**: Проверка загрузки модели с простой генерацией
- 📋 **Логирование**: Подробные логи для отладки проблем
- 🔄 **Автоматический fallback**: Несколько уровней резервных моделей

## 🚀 Возможности

- **Текстовая генерация**: Ответы на вопросы с поддержкой русского языка
- **REST API**: FastAPI с полной диагностикой и обработкой ошибок  
- **Веб-интерфейс**: Gradio UI с диагностическими панелями
- **Легкая модель**: Qwen2.5-0.5B быстро загружается и работает
- **Надежность**: Множественные fallback модели для гарантированной работы

## 📁 Структура проекта

```
.
├── app/
│   ├── main.py          # FastAPI с диагностическими эндпоинтами
│   ├── model.py         # Загрузка модели с fallback и логированием
│   └── ui.py            # Gradio UI с диагностикой
├── test_system.py       # Скрипт тестирования системы
├── Dockerfile
├── docker-compose.yml
├── requirements.txt     # Git версия transformers
└── README.md
```

## 🛠 Технологии

- **Qwen2.5-Omni-3B** + fallback на **Qwen2.5-3B**
- **Transformers** (dev версия из git)
- **FastAPI** с диагностическими эндпоинтами
- **Gradio** с улучшенным UI
- **PyTorch** с поддержкой CPU/GPU
- **Docker** для изоляции зависимостей

## 📋 Быстрый старт

### 🧪 Тестирование системы (рекомендуется)

Сначала запустите диагностику:

```bash
python test_system.py
```

Этот скрипт проверит:
- Импорты и версии библиотек
- Совместимость transformers
- Доступность GPU/CUDA
- Авторизацию HuggingFace
- Компоненты приложения

### 🐳 Запуск через Docker (рекомендуется)

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd lllm

# Запустите систему
docker-compose up --build
```

### 💻 Локальная установка

```bash
# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или .\venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt

# Запустите API
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# В отдельном терминале запустите UI
python app/ui.py
```

## 🌐 Доступ к сервисам

После запуска:
- **🏠 Главная**: http://localhost:8000
- **📋 API Docs**: http://localhost:8000/docs
- **💬 Веб-интерфейс**: http://localhost:7860
- **🔧 Диагностика**: http://localhost:8000/health

## 🔧 Диагностические эндпоинты

### GET /health
Проверка состояния API и загруженной модели

```bash
curl http://localhost:8000/health
```

### GET /model-info
Подробная информация о модели и системе

```bash
curl http://localhost:8000/model-info
```

### GET /test-model
Тестирование загрузки модели

```bash
curl http://localhost:8000/test-model
```

## 🎨 Веб-интерфейс

Веб-интерфейс включает:

1. **💬 Текстовый чат**: 
   - Генерация ответов с настраиваемым системным промптом
   - Детальная статистика ответов
   - Автоматический fallback на тестовый режим

2. **🔧 Диагностика**: 
   - Проверка статуса API и модели
   - Тестирование загрузки модели
   - Информация о системе и ошибках

## 🚨 Устранение неполадок

### Если модель не загружается:

1. **Проверьте версию transformers**:
   ```bash
   pip show transformers
   # Должна быть dev версия из git
   ```

2. **Обновите transformers**:
   ```bash
   pip install git+https://github.com/huggingface/transformers.git
   ```

3. **Проверьте авторизацию HuggingFace**:
   ```bash
   huggingface-cli login
   ```

### Если ошибка "qwen2_5_omni architecture not recognized":

1. Система автоматически переключится на `Qwen2.5-3B`
2. Проверьте логи в `/health` эндпоинте
3. Используйте диагностическую панель в UI

### Если нехватка памяти:

1. Система автоматически попробует CPU
2. Можно принудительно установить `device_map="cpu"` в `model.py`
3. Закройте другие приложения, использующие GPU

### Если ошибки импорта:

1. Запустите `python test_system.py`
2. Установите недостающие зависимости из `requirements.txt`
3. Проверьте виртуальное окружение

## 📊 Логирование и мониторинг

### Логи API:
```bash
# При запуске через Docker
docker-compose logs api

# При локальном запуске
# Логи выводятся в консоль с эмодзи и детальной информацией
```

### Логи UI:
```bash
# При запуске через Docker
docker-compose logs ui

# При локальном запуске
# Логи выводятся в консоль
```

## 🔧 Настройка

### Смена модели:
В файле `app/model.py` измените `model_name`:
```python
model_name = "Qwen/Qwen2.5-7B"  # Для более мощной версии
# или
alt_model_name = "Qwen/Qwen2.5-1.5B"  # Для более легкой версии
```

### Настройка параметров генерации:
В `app/main.py` измените параметры в функции `generate()`:
```python
max_new_tokens=1024,  # Больше токенов
temperature=0.8,      # Более креативные ответы
```

### Настройка логирования:
```python
# В app/model.py или app/main.py
logging.basicConfig(level=logging.DEBUG)  # Больше деталей
# или
logging.basicConfig(level=logging.WARNING)  # Меньше логов
```

## 📚 Полезные команды

```bash
# Тестирование системы
python test_system.py

# Проверка статуса API
curl http://localhost:8000/health | jq

# Тестовый запрос к API
curl -X POST http://localhost:8000/simple-chat \
  -H "Content-Type: application/json" \
  -d '{"text": "Привет!"}'

# Полный запрос к API
curl -X POST http://localhost:8000/generate \
  -H "Content-Type: application/json" \
  -d '{"text": "Расскажи о квантовой физике"}'

# Проверка логов Docker
docker-compose logs -f api
docker-compose logs -f ui
```

## 📈 Мониторинг производительности

API автоматически предоставляет метрики:
- Время генерации
- Длина входа/выхода
- Использование памяти
- Статус устройств (CPU/GPU)

## 📄 Лицензия

Проект использует модель Qwen2.5-Omni-3B. Ознакомьтесь с лицензией модели на HuggingFace.

## 🤝 Поддержка

При проблемах:
1. Запустите `python test_system.py`
2. Проверьте логи через диагностические эндпоинты
3. Используйте диагностическую панель в UI
4. Изучите детальные логи с эмодзи в консоли